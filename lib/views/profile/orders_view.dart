import 'package:flutter/material.dart';

class PedidosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Mis pedidos')),
      body: Center(child: Text('Contenido de Mis pedidos')),
    );
  }
}
